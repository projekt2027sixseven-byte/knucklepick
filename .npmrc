; When NODE_ENV=production, npm defaults to omit=dev and skips devDependencies.
; Next.js production builds still need workspace devDependencies (Tailwind, PostCSS, types, etc.).
; Explicit omit=optional keeps devDependencies while still omitting only optional deps.
omit=optional
